import styled from "styled-components"

const QuizWrapper=styled.div`
    font-family: 'SUIT-Regular';
    margin-top: calc(50px + 1%);
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
`
const Quiz = () => {
  return (
    <QuizWrapper>Quiz</QuizWrapper>
  )
}

export default Quiz